return {
  version = 0;
  enabled = true;
  message = 'Hello World!';

  -- Basic settings for new file

  -- Unlock all arcana and grasp from the beginning
  starting_grasp = 30;
  unlock_arcana = true;
}